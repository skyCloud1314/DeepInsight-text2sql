# 实施计划：Plotly图表Key冲突修复

## 概述

修复 Streamlit 应用中 Plotly 图表ID冲突问题，通过为每个图表添加唯一的key参数来解决"multiple plotly_chart elements with the same auto-generated ID"错误。

## 任务

- [x] 1. 创建图表key生成工具函数
  - 实现 `generate_chart_key()` 函数，基于上下文生成唯一标识符
  - 实现 `get_data_hash()` 函数，为数据生成哈希值
  - 实现 `create_chart_with_key()` 统一图表渲染函数
  - _需求：3.1, 3.2, 3.3, 3.4, 3.5_

- [ ]* 1.1 编写key唯一性属性测试
  - **属性1：Key唯一性**
  - **验证需求：需求1.1, 1.2, 1.3**

- [x] 2. 修复侧边栏性能趋势图的key冲突
  - 为 `app.py` 第714行的 `st.plotly_chart()` 添加唯一key
  - 使用 "sidebar_performance_trend" 作为key前缀
  - 包含时间范围参数在key中以区分不同配置
  - _需求：1.1, 1.2, 2.1, 2.2_

- [ ]* 2.1 编写key一致性属性测试
  - **属性2：Key一致性**
  - **验证需求：需求1.3, 2.4**

- [x] 3. 修复历史消息中的图表key冲突
  - 为 `app.py` 第1307行和1311行的 `st.plotly_chart()` 添加唯一key
  - 基于消息索引和图表类型生成key
  - 格式：`"history_chart_{msg_index}_{chart_type}"`
  - _需求：1.1, 1.5, 2.1, 2.3_

- [ ]* 3.1 编写功能保持性属性测试
  - **属性3：功能保持性**
  - **验证需求：需求2.1, 2.2, 2.3**

- [x] 4. 修复新查询结果中的图表key冲突
  - 为 `app.py` 第2001行和2005行的 `st.plotly_chart()` 添加唯一key
  - 基于查询内容哈希和图表类型生成key
  - 格式：`"query_chart_{query_hash}_{chart_type}"`
  - _需求：1.1, 1.5, 2.1, 2.4_

- [ ]* 4.1 编写上下文区分性属性测试
  - **属性4：上下文区分性**
  - **验证需求：需求1.5, 3.3, 3.4**

- [x] 5. 实现图表上下文管理器
  - 创建 `ChartContext` 类来管理图表的来源和位置信息
  - 集成到现有的图表渲染流程中
  - 确保不同上下文的图表有不同的key前缀
  - _需求：3.1, 3.3, 3.4, 3.5_

- [ ]* 5.1 编写集成测试
  - 测试多图表页面的完整渲染流程
  - 测试历史消息和新查询图表的共存
  - _需求：1.1, 1.2, 1.5, 2.1_

- [x] 6. 添加错误处理和调试支持
  - 实现key冲突检测和自动解决机制
  - 添加详细的日志记录用于调试
  - 提供降级策略确保图表始终能显示
  - _需求：1.4, 2.1, 2.2_

- [x] 7. 最终检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户

## 注意事项

- 标记为 `*` 的任务是可选的，可以跳过以加快MVP开发
- 每个任务都引用了具体的需求以便追溯
- 属性测试验证通用的正确性属性
- 单元测试验证具体的示例和边界情况
- 修复应该保持所有现有图表功能的完整性，同时解决ID冲突问题